services:
  - name: telegram-expense-bot
    type: web
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py

jobs:
  - name: init-db-job
    type: job
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python db_init.py
